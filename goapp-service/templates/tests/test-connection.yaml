apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "goapp-service.fullname" . }}-test-connection"
  labels:
    {{- include "goapp-service.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "goapp-service.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
